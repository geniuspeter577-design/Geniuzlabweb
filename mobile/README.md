# Mobile

This directory is reserved for native or cross-platform mobile clients. No
mobile application exists yet.

Before implementation, define the API contract, authentication/session model,
notifications strategy, upload behavior, and offline/error handling. Mobile
clients should consume versioned backend APIs rather than import Django code.
